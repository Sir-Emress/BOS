# Quickstart Guide

Welcome to the project! Follow these steps to get running quickly:

1. **Clone the repo**: `git clone <url>`
2. **Install dependencies**: Use your package manager (e.g. `npm install` or `pip install -r requirements.txt`).
3. **Run the server**: Follow the specific run command in the main README.

Happy coding!
